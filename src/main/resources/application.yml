spring:
  application:
      name: translator

logging:
  level:
    root: info

application:
  properties:
    logging:
      logRequestForUrls: /*

---
spring:
  profiles: local

security:
  basic:
    enabled: false

management:
  security:
    enabled: false

server:
  port: 8977


